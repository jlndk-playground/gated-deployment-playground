on:
  workflow_dispatch:
  # push:
  #   branches:
  #     - master
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}

jobs:
  dryRun:
    runs-on:
      labels: ubuntu-latest
    steps:
      - run: |
          echo 'calculating what is gonna happen'
  deploy:
    needs: dryRun
    runs-on:
      labels: ubuntu-latest
    environment: production
    steps:
      - run: |
          echo 'deploying to production'
